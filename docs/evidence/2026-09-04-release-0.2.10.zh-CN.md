# PatchXNote Agent 0.2.10 Release Evidence

**Date:** 2026-09-04
**Version:** `0.2.10`
**Tag:** `v0.2.10`
**Commit:** `cbbd9a590b3d0ab1c1ab8b2c19fe4754e46cd44d`

## Summary

`patchxnote-agent@0.2.10` was released to GitHub Releases and published to npm. This release adds the PatchXNote MCP Skill, marketplace/package draft materials, MCP Registry metadata, and release validation scripts while preserving the existing local stdio MCP runtime shape.

This evidence proves GitHub Release asset creation, npm Trusted Publishing, npm registry availability, Windows published-package install smoke, local stdio MCP smoke, and repository-side skill/package validators. It does not claim OpenAI/Claude marketplace approval, official MCP Registry publication, Smithery publication, Cursor/Claude clean-client acceptance, or hosted platform acceptance.

## Release Inputs

- GitHub repository: `https://github.com/ZsTs119/patchxnote-agent`
- Release tag: `v0.2.10`
- Release URL: `https://github.com/ZsTs119/patchxnote-agent/releases/tag/v0.2.10`
- npm package: `patchxnote-agent`
- npm registry version after publish: `0.2.10`
- npm latest after publish: `0.2.10`
- npm `mcpName`: `io.github.zsts119/patchxnote-agent`

## GitHub Release

- Workflow: `.github/workflows/release.yml`
- Run: `33846214903`
- URL: `https://github.com/ZsTs119/patchxnote-agent/actions/runs/33846214903`
- Conclusion: `success`
- Started: `2026-09-04T06:52:45Z`
- Completed: `2026-09-04T06:54:22Z`

Release assets verified:

```text
checksums.txt
patchxnote_0.2.10_darwin_amd64
patchxnote_0.2.10_darwin_arm64
patchxnote_0.2.10_linux_amd64
patchxnote_0.2.10_linux_arm64
patchxnote_0.2.10_windows_amd64.exe
patchxnote_0.2.10_windows_arm64.exe
```

## npm Publish

- Workflow: `.github/workflows/publish-npm.yml`
- Run: `33846367389`
- URL: `https://github.com/ZsTs119/patchxnote-agent/actions/runs/33846367389`
- Conclusion: `success`
- Started: `2026-09-04T06:54:52Z`
- Completed: `2026-09-04T06:55:26Z`

Registry check:

```text
version = '0.2.10'
dist-tags.latest = '0.2.10'
mcpName = 'io.github.zsts119/patchxnote-agent'
```

Published package integrity:

```text
dist.shasum = '69da1f1bdc020fd8eec5ddcf30bd529373cc7403'
dist.integrity = 'sha512-XqoRqjV8ReUJCUItISjmc+PP0Der3qPoI27xCjcJUpUNtUO6FYgDboCrDYkkEOWF4QnUF62dIBT8n10cfLQ2yQ=='
```

## Local Validation Before Tag

Passed before `release 0.2.10` commit and tag:

```sh
node scripts/sync-patchxnote-skill-packages.mjs --check
node scripts/validate-patchxnote-skill-packages.mjs
python3 /mnt/c/Users/11979/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/patchxnote-mcp
python3 /mnt/c/Users/11979/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py packages/plugins/openai/patchxnote-agent
node docs/mcp-clients/validate-clients.mjs
node packages/npm/test/install.test.js
npm pack --dry-run
go test ./... -parallel 1
git diff --check
```

## Post-Publish Smoke

### npm package metadata

`npm view patchxnote-agent@0.2.10 version dist-tags.latest repository.url --registry https://registry.npmjs.org` returned:

```text
version = '0.2.10'
dist-tags.latest = '0.2.10'
repository.url = 'git+https://github.com/ZsTs119/patchxnote-agent.git'
```

### MCP config

`npx -y --registry https://registry.npmjs.org patchxnote-agent@0.2.10 mcp config` returned a generic secret-free stdio config using:

```text
npx -y patchxnote-agent@latest mcp serve
```

### Clean profile status

`npx -y --registry https://registry.npmjs.org patchxnote-agent@0.2.10 mcp status --profile release-0.2.10-clean --output json` returned:

```json
{
  "authenticated": false,
  "profile": "release-0.2.10-clean",
  "server_base_url": "https://ws-lab.patch-x.cn/patchnote-test-api",
  "client_id": "patchxnote-local-dev",
  "reason": "no_credential"
}
```

### Cursor setup dry-run

`npx -y --registry https://registry.npmjs.org patchxnote-agent@0.2.10 setup --client cursor --dry-run --print-config` passed and reported no file changes.

### Windows install smoke

`npx -y --registry https://registry.npmjs.org patchxnote-agent@0.2.10 install --install-dir <temp>` downloaded and installed the Windows amd64 binary. `patchxnote.exe version --output json` returned:

```json
{
  "version": "0.2.10",
  "commit": "cbbd9a590b3d0ab1c1ab8b2c19fe4754e46cd44d",
  "date": "2026-09-04T06:53:16Z",
  "go_version": "go1.22.12",
  "os": "windows",
  "arch": "amd64"
}
```

### Local stdio MCP smoke

`node scripts/smoke-mcp-stdio.mjs` launched:

```text
npx -y patchxnote-agent@latest mcp serve
```

The smoke result:

- `initialize`: success
- `tools/list`: success
- tool count: `19`
- `patchxnote_get_current_user`: present and returned `isError:false`
- `patchxnote_list_memories` with `{"platform":"mobile","limit":5}`: present and returned `isError:false`

The smoke script intentionally prints only structural summaries and does not print account details, memory content, OAuth codes, access tokens, refresh tokens, or webhook secrets.

## Skill Distribution Smoke

- `git ls-remote --tags origin refs/tags/v0.2.10` returned `cbbd9a590b3d0ab1c1ab8b2c19fe4754e46cd44d`.
- GitHub API confirmed `skills/patchxnote-mcp/SKILL.md` exists at tag `v0.2.10` with size `3570`.
- `npx -y skills add ZsTs119/patchxnote-agent --skill patchxnote-mcp --list` reached repository cloning but was interrupted after prolonged network slowness on this host. Count this as pending full remote skills CLI smoke, not as a skill content failure.

## Known Boundaries

- OpenAI/Codex plugin package is local-validator clean, but not submitted or approved.
- Claude Code package exists, but this host does not have `claude` CLI for install smoke.
- Official MCP Registry metadata exists, but official `mcp-publisher` binary validation/publish is still pending.
- Smithery metadata is a draft; hosted URL publishing requires Streamable HTTP and OAuth if auth is required.
- Current mounted Codex PatchXNote MCP tool set previously returned `invalid_params`; npm latest stdio smoke is the accepted evidence for this release.
