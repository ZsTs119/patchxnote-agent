# PatchXNote Agent 0.2.8 Release Evidence

**Date:** 2026-08-27

**Version:** `0.2.8`

**Commit:** `a933b668e9484e683441b78cf2e20b903bbe528a`

**Tag:** `v0.2.8`

## Result

`patchxnote-agent@0.2.8` was released to GitHub Releases and published to npm. Registry package smoke, Windows install smoke, Linux release-asset checksum smoke, and macOS install/MCP smoke passed.

The published package can read an existing Windows MCP OAuth credential and proxy stdio MCP calls to the remote `/mcp` endpoint. A fresh published-package browser OAuth login attempt opened or provided the browser authorization path, but the user did not complete the callback during the 10-minute window, so that fresh-login acceptance remains pending and must be repeated before claiming full browser-login release acceptance.

## GitHub Release

- Release workflow: `release.yml`
- Run id: `33061134986`
- Result: `success`
- Head: `a933b668e9484e683441b78cf2e20b903bbe528a`
- Tag: `v0.2.8`

Assets verified present:

```text
checksums.txt
patchxnote_0.2.8_darwin_amd64
patchxnote_0.2.8_darwin_arm64
patchxnote_0.2.8_linux_amd64
patchxnote_0.2.8_linux_arm64
patchxnote_0.2.8_windows_amd64.exe
patchxnote_0.2.8_windows_arm64.exe
```

## npm Publish

- npm publish workflow: `publish-npm.yml`
- Run id: `33061371378`
- Result: `success`
- npm package: `patchxnote-agent`
- npm latest after publish: `0.2.8`
- npm provenance: GitHub Actions signed provenance was emitted.
- Sigstore transparency log index: `2615699400`

## Windows Registry Package Smoke

Accepted from a normal Windows temporary directory, not from a WSL UNC working directory.

Passed:

- `npm view patchxnote-agent version dist-tags.latest repository.url --registry https://registry.npmjs.org`
  - `version=0.2.8`
  - `dist-tags.latest=0.2.8`
  - repository points to `github.com/ZsTs119/patchxnote-agent`
- `npx -y --registry https://registry.npmjs.org patchxnote-agent@0.2.8 install --install-dir <temp>`
  - installed `patchxnote.exe`
  - `patchxnote version --output json` returned version `0.2.8`, commit `a933b668e9484e683441b78cf2e20b903bbe528a`, OS `windows`, arch `amd64`
- `npx -y --registry https://registry.npmjs.org patchxnote-agent@0.2.8 mcp config`
  - stdout parsed as pure JSON
- `npx -y --registry https://registry.npmjs.org patchxnote-agent@0.2.8 mcp status --install-dir <temp> -- --profile mcp-oauth-registry-acceptance --server-base-url <test-server> --output json`
  - returned `authenticated=false`, `reason=no_credential`
- `npx -y --registry https://registry.npmjs.org patchxnote-agent@0.2.8 mcp status --install-dir <temp> -- --profile mcp-oauth-windows-acceptance --server-base-url <test-server> --output json --verify`
  - returned `authenticated=true`, `verified=true`, and 10 scopes using a previously accepted Windows credential
- `npx -y --registry https://registry.npmjs.org patchxnote-agent@0.2.8 mcp serve --install-dir <temp> -- --profile mcp-oauth-windows-acceptance --server-base-url <test-server>`
  - `initialize` passed with protocol `2026-07-28`
  - `tools/list` returned 19 tools
  - `tools/call patchxnote_get_current_user` returned active account projection with masked phone only
- `setup --client cursor --dry-run --print-config`
  - passed
  - no secret-like string was detected
- `install --dry-run --print-config`
  - passed
  - no secret-like string was detected

Observed caveat:

- Windows download of the GitHub Release asset was slow from the current network. `curl.exe` showed approximately 10MB body transfer from GitHub release assets taking several minutes. The install eventually succeeded.

## Linux Release Asset Smoke

Passed:

- Downloaded `checksums.txt`.
- Downloaded `patchxnote_0.2.8_linux_amd64`.
- `sha256sum -c` passed.
- `patchxnote version --output json` returned version `0.2.8`, commit `a933b668e9484e683441b78cf2e20b903bbe528a`, OS `linux`, arch `amd64`.

## macOS Install Smoke

- Workflow: `macOS Install Smoke`
- Run id: `33064643849`
- Result: `success`
- Head: `a933b668e9484e683441b78cf2e20b903bbe528a`

The workflow installed `patchxnote-agent@0.2.8` from npm, validated the macOS binary version, ran native keychain integration smoke, and confirmed MCP stdio initialize/tools list with 19 tools.

## Pending Acceptance

- Fresh published-package browser OAuth login on profile `mcp-oauth-registry-acceptance`: pending. The login process printed/opened the authorization path but timed out because no loopback callback arrived within 10 minutes.
- Real VS Code / Cursor / Codex / Claude / Windsurf / Trae / Qoder / WorkBuddy client UI acceptance: pending per client.
- Feishu Aily / Doubao Work Partner / Tencent Agent Platform / enterprise WorkBuddy hosted platform-console acceptance: pending.

## Sensitive Data Review

No raw phone number, OTP, OAuth code, PKCE verifier, access token, refresh token, webhook secret, raw audio, full transcript, source text dump, provider payload, full MAC, SK, or real platform credential is recorded in this evidence note.
