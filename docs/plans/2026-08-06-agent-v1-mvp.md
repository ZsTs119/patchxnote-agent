# Agent V1 MVP Implementation Plan

**Goal:** Build the smallest runnable PatchXNote Agent that can be installed, log in with an agent-only read session, serve a local stdio MCP server, and expose safe read-only PatchXNote account tools.

**Architecture:** Keep the Agent as a local Go binary plus an npm installer wrapper. MCP runs locally over stdio. PatchXNote Go Server remains the source of truth and gets only additive agent auth/read projection changes so existing App/PC flows stay unchanged.

**Tech Stack:** Go, Cobra, Viper, OS-native keychain storage, PatchXNote Go Server APIs, local stdio MCP, npm installer wrapper.

**Execution Rule:** Work sequentially in the primary agent. Do not use sub-agents or parallel task execution. Keep each step small, testable, and easy to revert.

---

## V1 Product Scope

V1 must close this user flow:

```text
npx -y patchxnote-agent install
patchxnote login
patchxnote auth status
patchxnote mcp serve
Agent client calls read-only PatchXNote MCP tools
patchxnote logout
```

V1 MCP tools:

- `patchxnote_get_current_user`
- `patchxnote_list_recorder_cards`
- `patchxnote_get_quota_summary`
- `patchxnote_get_model_usage_summary`
- `patchxnote_list_memories`
- `patchxnote_search_memories`
- `patchxnote_get_memory`

`patchxnote_search_memories` may use local search over authorized cached projections in V1. Do not require GoServer full-text search for the first closed loop unless the server already has a safe read projection that can be reused without broadening App/PC contracts.

V1 does not include:

- recorder-card battery, live BLE state, storage, or recording status
- hardware bind, release, recover, reset, or format
- SK/credential read/write
- raw audio download
- complete raw or corrected transcript access
- model-run execution
- quota purchase, reward claim, or payment
- delete/update content
- Admin APIs
- background sync workers
- multi-account or workspace management

## GoServer Bypass Safety Rules

Agent support is a sidecar path. It must not become a third App/PC platform and must not loosen existing client handlers.

Hard rules:

- Do not add `agent` to `identity.Platform`.
- Do not insert agent rows into `client_installation`.
- Do not call or wrap `POST /v1/installations` or `POST /v1/installations/replacements` from Agent login.
- Do not let agent tokens parse as normal `clientaccess.Actor` tokens.
- Do not make existing App/PC write handlers accept agent actors.
- Do not route Agent through hardware connect/release, model execute, quota claim, payment, content delete, namespace clear, or Admin handlers.
- Do expose V1 Agent data through a small `/v1/agent/...` read-only projection surface.
- Do require explicit content platform selection and matching scope for `mobile` or `desktop` structured results.
- Do keep hardware projection narrower than App/PC hardware list; mask full MAC by default and never return SK/credential.
- Do prove old App/PC mobile/desktop login, replacement, refresh, logout, hardware, quota, model, and content flows are unchanged.

## Definition Of Done

- a fresh user can install the CLI through the npm wrapper
- `patchxnote version` prints version/build metadata
- `patchxnote login` completes against a development PatchXNote server using phone OTP
- agent login does not create, replace, or revoke mobile/desktop installations
- GoServer does not create `PlatformAgent` or agent `client_installation` rows
- Agent tokens are rejected by existing App/PC write handlers
- V1 Agent server APIs live behind dedicated read-only `/v1/agent/...` routes
- structured-result access is explicitly scoped to either `mobile` or `desktop`
- credentials are stored through secure storage, not MCP config files
- `patchxnote auth status` shows the current account projection
- `patchxnote logout` revokes/removes local agent credentials
- `patchxnote mcp serve` speaks stdio MCP without non-JSON logs on stdout
- MCP tool listing exposes only the seven V1 tools
- every V1 tool has schema validation, bounded output, stable errors, and read-only scope checks
- old App/PC login and read flows still pass regression tests
- docs explain install, login, MCP config, troubleshooting, and V1 limitations

## Checklist

### Phase 0: Baseline And Planning

- [x] Commit the current repository rules and this plan.
- [x] Confirm the public GitHub repo remains `https://github.com/ZsTs119/patchxnote-agent`.
- [x] Confirm the npm package name target and expected first command.
- [x] Confirm npm package ownership validation is a pre-release gate, not a coding blocker.
- [x] Confirm the first supported MCP config target. Start with `--print-config` if client auto-install is uncertain.
- [x] Record the PatchXNote Go Server commit/OpenAPI version used as the V1 contract baseline.
- [x] Confirm whether V1 `patchxnote_search_memories` is local-cache search or a dedicated GoServer search projection. Default to local-cache search.

Phase 0 decisions:

- GitHub repository: `https://github.com/ZsTs119/patchxnote-agent`
- GitHub visibility: public
- Default branch: `main`
- npm package target: `patchxnote-agent`
- expected first command: `npx -y patchxnote-agent install`
- npm registry status on 2026-08-07: `patchxnote-agent` returned `E404 Not Found`
- npm local auth status on 2026-08-06: not logged in; scope ownership must be verified before first public npm publish
- first MCP client setup target: print-only/manual config via `--print-config`; client-specific auto-write comes after tested configs
- GoServer baseline commit: `c2479e9bfe8aa14fe40069718c28720f3a992b96`
- GoServer integration baseline: OpenAPI `0.20.2` in `docs/integrations/apifox/integration-guide.zh-CN.md`
- recorder-card baseline: OpenAPI `0.20.2`
- voice AI model flow baseline: candidate OpenAPI `0.19.0`; must be rechecked against target environment before implementation
- V1 memory search default: local search over authorized local cache, not a required GoServer full-text endpoint

### Phase 1: GoServer Agent Access Design

Files:

- Create or modify in GoServer: `docs/engineering/agent-access-v1.md`
- Modify in GoServer as needed: OpenAPI/integration docs for agent auth and read projections
- Modify in GoServer as needed: `tests/smoke/registry.yaml`

Checklist:

- [x] Define `agent` audience/session as independent from `mobile` and `desktop`.
- [x] Explicitly reject adding `agent` to `identity.Platform`.
- [x] Explicitly reject storing agent sessions in `client_installation`.
- [x] Explicitly reject using `/v1/installations` and `/v1/installations/replacements` for Agent login.
- [x] Decide the minimal OTP binding path for Agent without creating mobile/desktop installation side effects.
- [x] Reuse existing phone OTP send/verify ceremony where practical.
- [x] Verify OTP in one server transaction that creates an agent read session.
- [x] Define an agent actor type that cannot be confused with `clientaccess.Actor`.
- [x] Define read-only scopes: account, hardware, quota, model usage, selected platform content.
- [x] Define revoke/logout behavior using simple `revoked_at` semantics.
- [x] Avoid new pending/exchanging/handshake state machines.
- [x] Avoid new background workers and cache consistency protocols.
- [x] Require dedicated `/v1/agent/...` read projection endpoints for V1.
- [x] Require existing App/PC handlers to keep accepting only existing App/PC client actors.
- [x] Define forbidden route matrix: hardware connect/release, quota claim, model execute, content delete, namespace clear, payment, Admin.
- [x] Define explicit content platform grants: `agent:content.read:mobile` and `agent:content.read:desktop`.
- [x] Define whether each content request must pass `platform` or whether setup stores one selected platform.
- [x] Define narrower recorder-card projection that masks full MAC by default and omits release helper fields.
- [x] Define V1 memory search as local-cache search unless a dedicated server projection is accepted.

Validation:

- [x] Design explicitly states that agent login does not consume App/PC installation slots.
- [x] Design lists every V1 endpoint and required scope.
- [x] Design lists every forbidden endpoint and expected `403`/`401` behavior for agent tokens.
- [x] Design lists every old App/PC regression that must remain green.
- [x] Design includes OpenAPI and `tests/smoke/registry.yaml` updates required for each new Agent endpoint.

### Phase 2: GoServer Agent Auth Implementation

Files:

- Prefer creating or extending a narrow GoServer agent access module instead of turning `clientaccess` into a mixed App/PC/Agent actor system
- Modify GoServer auth/application/repository layers according to existing architecture only where reuse is safe
- Modify GoServer OpenAPI/integration docs
- Add GoServer tests near existing auth/session tests

Checklist:

- [x] Add failing test: agent OTP verify creates an agent session.
- [x] Add failing test: agent OTP verify does not create a `client_installation` row.
- [x] Add failing test: agent login does not replace mobile installation.
- [x] Add failing test: agent login does not replace desktop installation.
- [x] Add failing test: agent token cannot call write endpoints.
- [x] Add failing test: agent token cannot parse as a normal App/PC `clientaccess.Actor`.
- [x] Add failing test: App/PC tokens cannot parse as agent actors.
- [x] Add failing test: existing mobile and desktop OTP/install/replacement behavior is unchanged.
- [x] Implement the minimal agent session persistence.
- [x] Implement agent token signing/validation using existing cryptographic infrastructure where possible, but with an agent-specific audience/type/verifier boundary.
- [x] Implement refresh/revoke only if needed for the login/logout loop.
- [x] Keep revoke semantics to `revoked_at`; do not add multi-step logout states.
- [x] Add audit/request metadata without logging phone numbers, tokens, OTP, full MAC, SK, audio, or transcripts.

Validation:

- [x] Run targeted auth tests.
- [x] Run App/PC login regression tests.
- [x] Run forbidden-route authorization tests for agent tokens.
- [x] Run GoServer smoke relevant to changed endpoints.

Phase 2 result:

- GoServer Agent auth slice implemented in `internal/agentaccess`.
- Agent auth live smoke PASS: `/home/zsts_119/patchxNoteGoServer/artifacts/smoke/runs/r9be7f596f8ba/evidence.json`.
- Agent login requires an existing account, creates only `agent_session`, and leaves App/PC `client_installation` untouched.

### Phase 3: GoServer Read Projections

Files:

- Modify GoServer read handlers/application/repository layers as needed
- Modify GoServer OpenAPI/integration docs
- Add GoServer tests near each affected module

Checklist:

- [x] Add `/v1/agent/me` current user read projection.
- [x] Add `/v1/agent/recorder-cards` read projection.
- [x] Add `/v1/agent/quota/summary` read projection.
- [x] Add `/v1/agent/model-usage/summary` read projection.
- [x] Add `/v1/agent/memories` list read projection.
- [x] Add `/v1/agent/memories/{memory_id}` detail read projection.
- [x] Do not require GoServer full-text memory search for V1. Keep `patchxnote_search_memories` as local-cache search unless Phase 1 accepts a dedicated server search endpoint.
- [x] Do not reuse existing App/PC HTTP handlers directly for Agent routes.
- [x] If reusing service/repository methods internally, wrap them with an Agent read adapter that enforces account ID, selected content platform, and read-only scope.
- [x] Keep every projection bounded with explicit limits and pagination where needed.
- [x] Return stable IDs, timestamps, states, and cursors. Phase 3 memory title/summary is intentionally metadata-only until GoServer adds a dedicated safe plaintext projection.
- [x] Do not return SK, full MAC by default, raw audio, complete transcripts, provider payloads, or user prompt payloads.
- [x] Do not return model capability execution payloads or permit `model-runs:execute`.
- [x] Do not return quota ledger detail unless it is deliberately accepted as a later read tool.
- [x] Do not include daily reward claim/status behavior in Agent quota projection.
- [x] Add each new Agent endpoint to OpenAPI and `tests/smoke/registry.yaml`.

Validation:

- [x] Run module tests for each projection.
- [x] Run agent-scope authorization tests for every endpoint.
- [x] Run App/PC response schema regression tests and confirm no App/PC endpoint schema changed.
- [x] Run forbidden-route tests confirming agent tokens fail on hardware write, quota write, model execute, content delete, namespace clear, payment, and Admin routes.

Phase 3 result:

- GoServer Agent read projections implemented in `internal/agentaccess`; existing App/PC handlers were not reused or changed.
- OpenAPI now registers five new Agent read operations and `tests/smoke/agent-access` covers them after Agent login.
- Agent read live smoke PASS: `/home/zsts_119/patchxNoteGoServer/artifacts/smoke/runs/rc45b736ddd91/evidence.json`.
- Memory V1 is safe metadata only. It does not decrypt result vault payloads or expose titles/summaries yet.

### Phase 4: CLI Skeleton

Files:

- Modify: `cmd/patchnote/main.go`
- Create: `internal/cli/root.go`
- Create: `internal/cli/version.go`
- Create: `internal/version/version.go`
- Add tests: `internal/cli/*_test.go`

Checklist:

- [x] Add Cobra and Viper dependencies.
- [x] Make `cmd/patchnote/main.go` call `internal/cli.Execute()`.
- [x] Implement `patchxnote version`.
- [x] Implement root help and global flags.
- [x] Set `SilenceUsage` and `SilenceErrors`.
- [x] Send result output to stdout and diagnostics to stderr.
- [x] Add command tests using a fresh command tree per test.

Validation:

- [x] `go test ./...`
- [x] `go run ./cmd/patchxnote version`
- [x] `go run ./cmd/patchxnote --help`

Phase 4 result:

- CLI skeleton implemented with a thin `cmd/patchnote` entrypoint, fresh Cobra command tree constructor, Viper-backed global flags, and build metadata in `internal/version`.
- Cobra default `completion` command is disabled to keep the public command surface aligned with V1 scope.
- Validation PASS on 2026-08-06.

### Phase 5: Config And Credential Boundary

Files:

- Create: `internal/config/`
- Create: `internal/keychain/`
- Create: `internal/auth/`
- Add tests under each package

Checklist:

- [x] Implement non-secret config path resolution for macOS, Windows, and Linux.
- [x] Implement `PATCHXNOTE_` env binding.
- [x] Implement config precedence tests.
- [x] Define keychain interface and fake test implementation.
- [x] Store refresh/access material only through keychain interface.
- [x] Add `patchxnote auth status` with unauthenticated output.
- [x] Add `patchxnote logout` local cleanup path.

Validation:

- [x] `go test ./...`
- [x] no credential-like values appear in config tests or fixtures

Phase 5 result:

- Non-secret config loading now has platform path resolution, `PATCHXNOTE_` env binding, and tested precedence across defaults, file, env, and flags.
- Local credential handling is behind `internal/keychain.Store`; tests use `MemoryStore`, the public runtime defaults to OS-native keychain storage, and the explicit insecure file store remains only for local smoke.
- `patchxnote auth status` and `patchxnote logout` are available and produce no credential material in output.
- Validation PASS on 2026-08-06, including `go test ./...`, CLI smoke for `auth status`/`logout`, and a config test/fixture sensitive-value scan.

### Phase 6: API Client

Files:

- Create: `internal/api/`
- Add tests: `internal/api/*_test.go`
- Add sanitized fixtures under `testdata/api/`

Checklist:

- [x] Implement base URL configuration.
- [x] Implement stable API error type.
- [x] Implement agent OTP send/verify client methods.
- [x] Implement auth status/current user method.
- [x] Implement logout/revoke method.
- [x] Implement server-backed read projection client methods for current user, recorder cards, quota summary, model usage summary, memories list, and memory detail.
- [x] Implement memory search over authorized local cache unless a dedicated server search projection was accepted in Phase 1.
- [x] Add timeout and bounded retry policy for safe idempotent reads only.
- [x] Redact tokens, phone numbers, full MAC, SK, audio, and transcript content from errors.

Validation:

- [x] `go test ./...`
- [x] fixture tests cover success, auth failure, forbidden scope, not found, pagination, and rate limit

Phase 6 result:

- `internal/api` now maps all V1 Agent auth/read endpoints with typed responses, idempotency-key enforcement for OTP calls, stable `api.Error`, and bounded retry only for retryable GET failures.
- `internal/cache` provides a minimal local metadata search boundary for future `patchxnote_search_memories`; it searches only authorized safe metadata fields.
- Sanitized API fixtures cover success, unauthorized, forbidden, not found, pagination, and rate limit retry behavior without token, SK, full MAC, audio, transcript, prompt, or provider payload fixtures.
- Validation PASS on 2026-08-06 with `go test ./...` and API fixture sensitive-value scan.

### Phase 7: Login Commands

Files:

- Create: `internal/cli/login.go`
- Create: `internal/cli/logout.go`
- Create: `internal/cli/auth_status.go`
- Modify: `internal/auth/`
- Add tests: `internal/cli/*_test.go`

Checklist:

- [x] Implement `patchxnote login` interactive phone OTP flow.
- [x] Add non-interactive flags only where safe, for example `--phone` and `--code`.
- [x] Do not echo OTP or token values in logs.
- [x] Store credentials through keychain after verify succeeds.
- [x] Implement `patchxnote auth status --output json`.
- [x] Implement `patchxnote logout` local cleanup and server revoke when online.
- [x] Keep offline logout local cleanup successful even if server revoke fails.

Validation:

- [x] `go test ./...`
- [ ] manual dev-server login smoke with a non-sensitive test account
- [x] logout then `auth status` reports unauthenticated

Phase 7 result:

- `patchxnote login` now supports interactive prompts and `--phone`/`--code`, sends Agent OTP request/verify through `internal/api`, and stores session material only through `internal/keychain.Store`.
- Runtime credential storage uses OS-native keychain storage by default; an explicit `auth.insecure_file_keychain` / `PATCHXNOTE_AUTH_INSECURE_FILE_KEYCHAIN=true` development fallback exists for local smoke and is not written to normal config as secret material.
- `patchxnote auth status --output json` can verify the current account projection when an API client is configured; `patchxnote logout` attempts server revoke and always removes local credentials when revoke fails.
- Validation PASS on 2026-08-06 for `go test ./...`, fake OTP/API/keychain command tests, no OTP/token output leak tests, and local `logout` -> `auth status` smoke. Real dev-server OTP smoke requires a non-sensitive test account or review-code channel and is carried into Phase 11 E2E.

### Phase 8: MCP Server

Files:

- Create: `internal/mcp/server.go`
- Create: `internal/mcp/tools/`
- Create: `internal/cli/mcp.go`
- Add tests: `internal/mcp/*_test.go`

Checklist:

- [x] Implement `patchxnote mcp serve`.
- [x] Ensure stdio stdout contains MCP JSON-RPC only.
- [x] Send diagnostics to stderr only.
- [x] Implement tool registry with the seven V1 tools.
- [x] Add schema validation for each tool.
- [x] Add auth-required error mapping.
- [x] Add output size limits.
- [x] Add pagination defaults for list/search tools.

Validation:

- [x] `go test ./...`
- [x] MCP protocol smoke: initialize, list tools, call unauthenticated tool
- [ ] MCP protocol smoke: call authenticated success tool

Phase 8 result:

- `patchxnote mcp serve` now speaks newline-delimited JSON-RPC over stdio, supports `initialize`, `ping`, `tools/list`, and `tools/call`, and suppresses stdout diagnostics.
- The seven V1 tools are listed with bounded JSON input schemas and read-only annotations; tool calls validate params before auth checks and return stable JSON-RPC errors for auth, unknown methods, unknown tools, and invalid params.
- Validation PASS on 2026-08-06 for `go test ./...`, local initialize/tools-list stdio smoke, and unauthenticated tool-call smoke. Authenticated success calls start in Phase 9 as each tool handler is implemented.

### Phase 9: V1 MCP Tools One By One

Files:

- Create or modify: `internal/mcp/tools/*.go`
- Modify: `internal/api/`
- Add tests: `internal/mcp/tools/*_test.go`

Checklist:

- [x] Implement `patchxnote_get_current_user`.
- [x] Implement `patchxnote_list_recorder_cards`.
- [x] Implement `patchxnote_get_quota_summary`.
- [x] Implement `patchxnote_get_model_usage_summary`.
- [x] Implement `patchxnote_list_memories`.
- [x] Implement `patchxnote_search_memories` against authorized local cache by default.
- [x] Implement `patchxnote_get_memory`.
- [x] After each tool, run focused tests and a protocol smoke before moving to the next one.
- [x] Keep tool responses short and structured.
- [x] Do not expose unavailable fields as guessed values.

Validation:

- [x] every tool has success, auth failure, forbidden scope, validation error, and server error tests
- [x] every list tool has limit and pagination tests
- [x] search has local-cache tests for no cache, no match, match, stale auth, and output limit behavior
- [x] no tool returns secret or disallowed content in snapshots

Phase 9 result:

- All seven V1 MCP tools now enforce Agent auth and scopes, call `internal/api` for server-backed projections, and return bounded JSON text content.
- `patchxnote_search_memories` uses only the in-process authorized metadata cache populated by list/get memory calls; it does not add a GoServer search endpoint or infer unavailable titles/summaries.
- Tool output excludes tokens, SK, full MAC, audio, transcripts, prompt/provider payloads, and guessed live recorder-card fields such as battery.
- Validation PASS on 2026-08-06 with `go test ./...`, focused seven-tool MCP success smoke, auth/scope/validation/server-error tests, output-size tests, and sensitive-value scan.

### Phase 10: npm Installer Wrapper

Files:

- Modify: `packages/npm/package.json`
- Modify: `packages/npm/bin/install.js`
- Add installer tests or dry-run script

Checklist:

- [x] Implement platform and architecture detection.
- [x] Implement pinned version resolution.
- [x] Download matching binary from GitHub release assets.
- [x] Verify checksum before installing.
- [x] Install to a user-writable path.
- [x] Add `--dry-run`.
- [x] Add `--print-config` or equivalent for manual MCP client setup.
- [x] Avoid admin/root requirement.
- [x] Fail with non-zero exit code on install failure.

Validation:

- [x] npm installer dry-run on Windows
- [ ] npm installer dry-run on Linux
- [ ] documented macOS validation before public promotion

Phase 10 result:

- `packages/npm` now exposes `patchxnote-agent install` with platform/arch detection, pinned version asset URL resolution, checksum verification, user-writable install path, `--dry-run`, `--print-config`, and `--from-local` for local smoke.
- Dry-run and print-config output contain no credential or MCP secret material. The wrapper remains `private: true` until Phase 12 release ownership and binary artifact gates pass.
- Validation PASS on 2026-08-06 for bundled Node installer tests, direct install dry-run, Windows npm `pack --dry-run`, and `go test ./...`.
- True Linux npm dry-run, macOS validation, and local `npx`-style smoke are carried into Phase 12 because this WSL workspace currently has Windows npm but no Linux node/npm, and Windows npm has UNC path limitations.

### Phase 11: End-To-End MVP Smoke

Files:

- Create: `docs/evidence/` only for sanitized local smoke notes if needed
- Modify: `README.md`

Checklist:

- [x] Build local binary.
- [x] Install through npm wrapper dry-run.
- [x] Login against safe in-process Agent V1 smoke server. Real deployed dev-GoServer OTP login remains a Phase 12/public-release environment gate.
- [x] Check `patchxnote auth status`.
- [x] Start `patchxnote mcp serve`.
- [x] Initialize MCP connection.
- [x] List tools.
- [x] Call all seven tools against safe development data.
- [x] Logout.
- [x] Confirm old App/PC login regression remains green.
- [x] Update README with tested commands and known limitations.

Validation:

- [x] `go test ./...`
- [x] GoServer targeted tests and smoke for agent endpoints
- [x] GoServer forbidden-route smoke confirms agent tokens cannot call existing App/PC write endpoints
- [x] MCP smoke transcript contains no secrets or user content

Phase 11 result:

- Local MVP smoke script `scripts/e2e/mvp-smoke.sh` builds the CLI, runs npm installer dry-run, logs in against an in-process Agent V1 test server, checks auth status, starts MCP stdio, initializes MCP, lists tools, calls all seven tools, logs out, and writes sanitized evidence.
- Latest local evidence: `.tmp/e2e/evidence.json` with status `pass`, `tool_count=7`, and no token/OTP/SK/full-MAC/audio/transcript/prompt/provider payload matches.
- GoServer agent/App-PC regression remained green after CLI integration checks. Latest GoServer smoke evidence: `/home/zsts_119/patchxNoteGoServer/artifacts/smoke/runs/r726d49f9f1a0/evidence.json`.
- Validation PASS on 2026-08-06 for `go test ./...`, `scripts/e2e/mvp-smoke.sh`, GoServer targeted unit/regression tests, GoServer agent/clientaccess integration tests, and `make smoke-module MODULE=agent-access`.

### Phase 12: First Public Release

Files:

- Create: `.github/workflows/`
- Create: `.goreleaser.yaml`
- Modify: `packages/npm/`
- Modify: `README.md`

Checklist:

- [x] Add release workflow for Go binaries.
- [x] Generate checksums for every artifact.
- [x] Publish npm wrapper only after binary release exists.
- [x] Verify install command on Linux and Windows from GitHub Release assets.
- [x] Add repeatable macOS install/keychain smoke workflow.
- [x] Run macOS install command on macOS runner for the release version.
- [x] Tag release from a clean commit.
- [x] Document rollback by pinned version.

Validation:

- [x] Linux fresh install smoke from GitHub Release assets.
- [x] Windows fresh install smoke from GitHub Release assets.
- [x] macOS source keychain integration smoke workflow added.
- [x] macOS fresh install smoke on macOS runner for the release version.
- [x] `patchxnote version` matches release tag and commit.
- [x] `patchxnote mcp serve` starts from installed binary.
- [x] npm `npx -y patchxnote-agent@0.2.0 install` smoke after npm publish.

Phase 12 real test-server acceptance result:

- [x] Deployed GoServer test runtime with Agent routes: `a1ddd63d8fff0255380dc0eb1b9ea77dfc430f30`, migration `36 dirty=false`, API/worker active, pprof closed, logs private-json.
- [x] Online OpenAPI is `0.20.3` and exposes 9 `/v1/agent/**` paths.
- [x] Real Agent OTP request returned `202 accepted`; verification returned an Agent session with 6 read scopes. The phone number, OTP, access token, and request correlation IDs are not recorded here.
- [x] CLI `auth status` against the real test server returned authenticated with an active account projection.
- [x] Real `patchxnote mcp serve` stdio smoke listed exactly 7 V1 tools.
- [x] Real MCP tool calls PASS: current user, recorder cards, quota summary, model usage summary, list memories, and local search memories.
- [x] Real recorder-card projection returned 5 masked recorder cards.
- [x] Real memory list returned 0 items for the selected platform, so `patchxnote_get_memory` had no safe sample object to call.
- [x] `patchxnote logout` called server logout and local `auth status` returned unauthenticated; temporary credential file was reduced to `{}`.
- [x] Text evidence privacy scan found no OTP, raw phone number, access token, Bearer token, SK, full MAC, audio, transcript, prompt, or provider payload.

Phase 12 public release status:

- [x] Release configuration added: `.goreleaser.yaml`, tag-triggered GitHub binary release workflow, GitHub artifact attestation step, and manual npm publish workflow gated on release assets.
- [x] npm wrapper prepared for public package versions `0.1.1` and `0.1.2`, including install/update/uninstall paths and official registry publish config.
- [x] GitHub Releases `v0.1.0`, `v0.1.1`, and `v0.1.2` published with six OS/arch binaries and `checksums.txt`.
- [x] Release asset checksums pass for all six binaries; GitHub artifact attestation verifies for sampled Linux and Windows binaries.
- [x] Linux and Windows installer smokes pass from real GitHub Release assets, including checksum verification, `patchxnote version`, and MCP initialize.
- [x] npm publish PASS on 2026-08-07. Official registry exposes `patchnote-agent@0.1.1`, and `npx -y --registry https://registry.npmjs.org patchnote-agent@0.1.1 install --dry-run --print-config` PASS.
- [x] Windows real install smoke from npm PASS: `npx -y --registry https://registry.npmjs.org patchnote-agent@0.1.1 install --install-dir <temp>` downloaded the GitHub Release binary, verified checksum, and `patchnote version` reported `0.1.1` at commit `8c82973d690b7ca58b79ddbab7d57e5a2a82f470`.
- [x] npm Trusted Publishing PASS for `patchnote-agent@0.1.2` on 2026-08-07: GitHub Actions run `31145600452`.
- [x] Windows install smoke from npm PASS for `0.1.2`: `patchnote version` reported commit `af03c697c9a326b12b67659db3af4053d4513093`.
- [x] macOS execution/keychain smoke PASS on macOS runner for `v0.1.2`: GitHub Actions run `31145626988`.
- [x] GitHub npm publish workflow migrated to npm Trusted Publishing/OIDC shape: `id-token: write`, no `NODE_AUTH_TOKEN`, and exact repository metadata in `packages/npm/package.json`.
- [x] npm-side trusted publisher configured on 2026-08-07 for `ZsTs119/patchxnote-agent`, workflow `publish-npm.yml`, permission `npm publish`.
- [x] Trusted Publishing smoke PASS on 2026-08-07: rerunning the publish workflow for existing version `0.1.1` reached npm publish through GitHub Actions OIDC and failed only with the expected immutable-version error, `You cannot publish over the previously published versions: 0.1.1`.
- [x] Public README benchmarked against Lark/OpenClaw, Gemini CLI, Codex CLI, GitHub MCP Server, Crush, and Stripe CLI patterns; added English/Chinese language switch, Chinese README, richer requirements, safety notice, troubleshooting, npm package README, and `docs/readme-benchmark.zh-CN.md`.
- [x] Public visual assets added for GitHub and Feishu use: cover, quickstart, architecture, safety boundary, tools capability, and social preview images under `docs/assets/`.
- [x] PatchXNote brand sync prepared for `0.1.3`: public README, npm wrapper display strings, OS-native keychain service naming, legacy keychain fallback, and GitHub/Feishu visual assets.
- [x] npm Trusted Publisher configured for the new `patchxnote-agent` package.
- [x] Removed the GitHub Actions `NPM_TOKEN` secret after OIDC verification.
- [ ] Revoke the old 90-day token from npm Access Tokens and disallow token-based publishing for this package.
- [x] Public beta promotion uses the current PatchXNote public-beta API environment and explicit public-beta wording.
- [x] `v0.1.1` beta candidate defaults the server base URL to `https://ws-lab.patch-x.cn/patchnote-test-api` and keeps credential file fallback explicit through `PATCHXNOTE_AUTH_INSECURE_FILE_KEYCHAIN=true`.
- [x] `v0.1.2` released and published on 2026-08-07. It adds native credential storage via macOS Keychain, Windows Credential Manager, and Linux Secret Service when available; the file fallback remains explicit for local smoke only.
- [x] `v0.1.3` released and published on 2026-08-07. It syncs public branding to PatchXNote, refreshes GitHub/Feishu visual assets, keeps the `patchnote-agent` npm package and `patchnote` binary names for install compatibility, and migrates native keychain reads from the legacy `patchnote-agent` service to `patchxnote-agent`.
- [x] Release workflow PASS for `v0.1.3`: GitHub Actions run `31146941050`; npm Trusted Publishing PASS: run `31147047484`; Windows npm install smoke PASS with `patchnote version` reporting commit `41b5c1cd3256e38bd27e11c7ff8cac0aa276094f`; macOS install/keychain smoke PASS: run `31147112856`.
- [x] `v0.2.0` rename candidate prepared on 2026-08-07: public entry changes to npm `patchxnote-agent`, binary `patchxnote`, MCP tools `patchxnote_*`, Go module `github.com/ZsTs119/patchxnote-agent`, and `PATCHXNOTE_` env prefix with legacy `PATCHNOTE_` fallback.
- [x] GitHub repository renamed to `https://github.com/ZsTs119/patchxnote-agent` and repository description updated for PatchX/PatchXNote public beta positioning.
- [x] Release workflow PASS for `v0.2.0`: GitHub Actions run `31149260230`; six OS/arch binaries and `checksums.txt` published under the new `patchxnote_*` asset names.
- [x] Linux release-asset smoke PASS for `0.2.0`: downloaded `patchxnote_0.2.0_linux_amd64`, verified checksum, `patchxnote version` reported commit `f20f1fe6d0bf5cd4ffc6b9dfe32e88ca2b0f8e97`, and `mcp serve` listed exactly 7 V1 tools.
- [x] npm first publish PASS for `patchxnote-agent@0.2.0` on 2026-08-07: GitHub Actions run `31149517967`; this first publish used `NPM_TOKEN` because npm Trusted Publishing cannot bootstrap a package before it exists.
- [x] Windows npm public install smoke PASS for `0.2.0`: `npx -y --registry https://registry.npmjs.org patchxnote-agent@0.2.0 install --install-dir <temp>` downloaded the GitHub Release binary, verified checksum, and `patchxnote version` reported commit `f20f1fe6d0bf5cd4ffc6b9dfe32e88ca2b0f8e97`.
- [x] macOS public install and MCP smoke PASS for `0.2.0`: GitHub Actions run `31149590517`; the installed `patchxnote` binary reported version `0.2.0`, `mcp serve` initialized over stdio, and tool listing returned exactly 7 V1 tools.
- [x] `v0.2.1` released and published on 2026-08-07 through npm Trusted Publishing/OIDC, with no `NODE_AUTH_TOKEN` in the publish workflow. Release workflow PASS: GitHub Actions run `31150215135`; npm Trusted Publishing PASS: run `31150311340`; npm provenance statement was signed from GitHub Actions.
- [x] Windows npm latest install smoke PASS for `0.2.1`: `npx -y --registry https://registry.npmjs.org patchxnote-agent install --install-dir <temp>` installed `patchxnote` and `patchxnote version` reported commit `341752097415f75205e5f15fcb43976774817ba9`.
- [x] macOS public install and MCP smoke PASS for `0.2.1`: GitHub Actions run `31150369171`; the installed `patchxnote` binary reported version `0.2.1`, `mcp serve` initialized over stdio, and tool listing returned exactly 7 V1 tools.

## Open Questions Before Coding

- Answered on 2026-08-07: `0.1.2` is the first new-version Trusted Publishing release after `0.1.1`.
- Which MCP client should get auto-config first: Codex, Claude Desktop, Cursor, or print-only config?
- Which development server environment should be used for OTP login smoke?
- Should setup ask for a structured-result platform, or should every content tool require explicit `platform`?
- Can any GoServer service/repository methods be reused behind `/v1/agent/...` without changing existing App/PC handlers?
- Is local-cache memory search enough for V1, or do we need a dedicated `/v1/agent/memories:search` endpoint later?
